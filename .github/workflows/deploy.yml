name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]  # 當推送到 main 分支時觸發
  workflow_dispatch:    # 允許手動觸發

permissions:
  contents: write       # 允許 Action 推送程式碼到 gh-pages 分支

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies 📦
        run: npm ci
        # 如果沒有 package-lock.json，請改用: npm install

      - name: Build 🏗️
        run: npm run build
        env:
          # 在這裡將 GitHub Secrets 注入到 Build 過程中
          # Vite 會在 build 時讀取這些變數並打包進 dist/
          API_KEY: ${{ secrets.API_KEY }}
          VITE_FINNHUB_API_KEY: ${{ secrets.VITE_FINNHUB_API_KEY }}
          VITE_FIREBASE_CONFIG_STRING: ${{ secrets.VITE_FIREBASE_CONFIG_STRING }}

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Vite 預設的打包輸出目錄
          branch: gh-pages # 佈署到的分支
