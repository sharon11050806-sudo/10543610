name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write # 必須設定，允許 Action 推送程式碼到 gh-pages 分支

jobs:
  build:
    name: Build Project
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20
          # 暫時不使用 cache，避免無 lock file 時報錯

      - name: Install Dependencies 📦
        run: npm install

      - name: Build App 🏗️
        run: npm run build
        env:
          # 在 Build 階段注入環境變數
          API_KEY: ${{ secrets.API_KEY }}
          VITE_FINNHUB_API_KEY: ${{ secrets.VITE_FINNHUB_API_KEY }}
          VITE_FIREBASE_CONFIG_STRING: ${{ secrets.VITE_FIREBASE_CONFIG_STRING }}

      - name: Upload Build Artifact 📤
        uses: actions/upload-artifact@v4
        with:
          name: production-files
          path: dist
          retention-days: 1

  deploy:
    name: Deploy to Branch
    needs: build # 必須等待 build 完成
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Download Artifact 📥
        uses: actions/download-artifact@v4
        with:
          name: production-files
          path: dist

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages # 將編譯結果推送到此分支
